@model Infrastructure.Models.ViewModels.LoginRequest;

@{
    ViewData["Title"] = "Login";
}

<div class="container mt-3" style="display: flex; height: 60vh;">
    <div class="card shadow-lg" style="width: 24rem; margin: auto;">
        <div class="card-body">
            <h3 class="card-title text-center mb-4">Đăng nhập</h3>
            <form asp-action="Login" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <!-- Email -->
                <div class="mb-3">
                    <label asp-for="Username" class="form-label">Username</label>
                    <input asp-for="Username" type="text" class="form-control" placeholder="Nhập username" required>
                    <span asp-validation-for="Username" class="text-danger"></span>
                </div>
                <!-- Password -->
                <div class="mb-3">
                    <label asp-for="Password" class="form-label">Password</label>
                    <input asp-for="Password" type="password" class="form-control" placeholder="Nhập password" required>
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>
                <!-- Remember Me -->
                <div class="mb-3 form-check">
                    <input type="checkbox" class="form-check-input" id="rememberMe">
                    <label class="form-check-label" for="rememberMe">Remember me</label>
                </div>
                <!-- Login Button -->
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Đăng nhập</button>
                </div>
                <div class="d-grid mt-3">
                    <a asp-action="LoginByGoogle" asp-controller="Authentication" class="btn btn-danger">
                        <i class="bi bi-google"></i> Đăng nhập với Google
                    </a>
                </div>
            </form>
            <!-- Forgot Password -->
            <div class="text-center mt-3">
                <a href="#" class="text-decoration-none">Quên mật khẩu?</a>
            </div>
            <hr>
            <!-- Sign Up Link -->
            <div class="text-center">
                <p>Don't have an account? <a asp-action="Signup" class="text-decoration-none">Đăng ký</a></p>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}