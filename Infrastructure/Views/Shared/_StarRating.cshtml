@model double


<div class="star-rating">
    @{
        for (int i = 1; i <= 5; i++)
        {
            <span class="star @(i <= Math.Round(Model) ? "filled" : "")">&#9733;</span>
        }
    }
</div>